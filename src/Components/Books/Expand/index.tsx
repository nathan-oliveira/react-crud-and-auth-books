import React from "react";

const BookExpand = ({ record }: any) => {
  // React.useEffect(() => {
  //   console.log(record)
  // }, []);

  return (
    <form>
      { record.id }
    </form>
  );
}

export default BookExpand;