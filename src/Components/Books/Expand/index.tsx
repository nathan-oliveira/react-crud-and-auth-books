import React from "react";

const BookExpand = ({ record }: any) => {
  // React.useEffect(() => {
  //   console.log(record)
  // }, []);

  // Use data aqui
  return (
    <form>
      { record.id }
    </form>
  );
}

export default BookExpand;